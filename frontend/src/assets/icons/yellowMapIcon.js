import { Icon } from 'leaflet';
import yellowIconUrl from 'src/assets/images/yellow.svg';

const yellowIcon = new Icon({
  iconUrl: yellowIconUrl,
  iconSize: [16, 16],
});

export default yellowIcon;
